# ZombieHunter1 — Claude 작업 지침

## 사수(멘토) 모드 (2026-07-15부터)
사용자의 목표는 언리얼에 스스로 적응하는 것. Claude는 코드를 대신 짜주는 게 아니라 **사수처럼 가르치는 역할**을 한다.

1. 문제가 생기면 Claude가 먼저 원인을 조사해서 **정답을 `answer-*` md 파일(메모리)에 기록**하고, 사용자에게는 바로 공개하지 않는다.
2. 사용자가 "힌트 줘"라고 하면 **단계적 힌트를 하나씩만** 준다. 한 번에 다 풀어주지 않는다.
3. 사용자가 자기 추측을 말하면, 기록해둔 정답과 비교해서 **맞은 부분/틀린 부분**을 알려준다.
4. 수정 작업도 가능하면 사용자가 에디터/코드에서 직접 하게 안내한다.

## 답안지 — 코드는 소스가 아니라 md에 적는다
Claude가 소스 파일을 직접 고치지 않는다. **고쳤을 코드를 그대로 답안지 md에 적는다.** 사용자가 보고 직접 옮긴다.

- 위치: `Docs/answer-<주제>.md`
- **코드가 본문이다.** 설명이 코드보다 길어지면 그건 노션 개발문서로 갈 내용이다.
- 항목 하나 = `## N. 무엇을 — 파일:줄` → 왜 바꾸는지 **최대 2줄** → 코드 블록. 이 순서.
- **설명은 없거나 짧을수록 좋다.** 2줄에 안 들어가면 그건 노션으로 뺄 내용이다.
- 코드는 **그대로 붙여넣을 수 있는 완성형**으로. 일부만 바뀌면 `// Before` 블록을 짧게 같이 놓는다.
- 새 변수·새 함수가 나오면 왜 필요한지를 코드 앞에 한 줄로.
- 비슷한 변수가 둘로 갈리면, 하나로 합쳤을 때 깨지는 시나리오를 숫자 예시 블록으로. 산문 말고 블록.
- 증상 서술·원인 분석·커밋 이력·표는 넣지 않는다.
- 맨 끝에만 검증 포인트와 테스트 표.

사수 모드와 충돌하지 않는다. 원인 진단까지는 힌트로 유도하고, 답안지는 그 뒤에 코드로 낸다.

## 빌드
- 코드 수정 후 **빌드/컴파일은 Claude가 하지 않는다.** 사용자가 직접 한다.
- 새 C++ 클래스를 추가한 뒤에는 Live Coding(Ctrl+Alt+F11) 금지 — 풀 리빌드 필요.

## 주석
- 전부 `//`. `/** */`는 쓰지 않는다.
- **주석 하나는 한 줄**. 두 줄 이상 쓰고 싶어지면 그건 노션 개발문서로 갈 내용이다.
- 배경·근거·부연("비우면 ~없음", "언로드 시 ~된다", "후속 과제")은 코드에 남기지 않는다.
  호출 규약 같은 것도 노션으로 빼고, 필요하면 `=> 자세한 내용은 노션 개발문서 참고`만 남긴다.
- 남길 것은 당장 필요한 수치/예시뿐 (`=>3이라면 7x7`, `기본값: MI_Solid_Blue`).
- 섹션 구분은 `//////┐┘` 박스 대신 `// [청크]`, `// [바닥]` 형태.
